// cell width in px
export const cellWidth = 20;
// maximum number of visible cells
export const maxVisibleCells = 40;
// image (bomb, flag) width in px
export const imageWidth = 16;
// maximum number of rendered items in one dimension
export const renderedLength = 160;
// visible viewport width in px
export const viewportWidth = maxVisibleCells * cellWidth;
